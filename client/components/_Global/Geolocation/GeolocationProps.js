import PropTypes from 'prop-types';

export const propTypes = {
  showGeolocationModal: PropTypes.bool,
  cityName: PropTypes.string,
  fetchGeolocation: PropTypes.func,
  toggleGeolocationModal: PropTypes.func,
};
